apiVersion: v1
kind: Service
metadata:
  name: devops-pipeline-demo-svc
spec:
  selector:
    app: devops-pipeline-demo
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP
